# System dependencies for this repo (macOS / Homebrew).
# Install everything with:  brew bundle
# These are the non-Python prerequisites; Python deps are managed by uv.

# Python project/dependency manager used by this repo.
brew "uv"

# Converts arXiv HTML to Markdown (maps MathML to LaTeX) for sync_library.py.
brew "pandoc"
